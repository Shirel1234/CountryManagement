export enum RequestAccessAction {
    ADD = "add",
    UPDATE = "update",
    DELETE = "delete",
  }
  
  export enum RequestAccessStatus {
    PENDING = "pending",
    APPROVED = "approved",
    DENIED = "denied",
  }
  